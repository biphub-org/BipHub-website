---
phase: 04-polish-static-content-performance-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(public)/privacy/page.tsx
  - components/home/Footer.tsx
autonomous: true
requirements: [FOUN-05, FOUN-06]
must_haves:
  truths:
    - "A user can navigate to /privacy from a link in the public Footer"
    - "The privacy page enumerates every storage surface used by the site (Supabase Auth cookies, biphub:bookmarks, bip-draft, BIP submission content, coordinator profile)"
    - "The page documents zero analytics scripts in v1 (FOUN-05 satisfied by absence-of-trackers, no consent banner needed)"
    - "Page is statically rendered and indexable"
  artifacts:
    - path: "app/(public)/privacy/page.tsx"
      provides: "Static RSC privacy policy page (~600-900 words)"
      contains: "export const dynamic = 'force-static'"
    - path: "components/home/Footer.tsx"
      provides: "Public footer with /privacy link added"
      contains: "href=\"/privacy\""
  key_links:
    - from: "components/home/Footer.tsx"
      to: "/privacy"
      via: "new <Link href=\"/privacy\">"
      pattern: "href=\"/privacy\""
---

<objective>
Ship the `/privacy` static privacy policy page (FOUN-06) and add a footer link to it. The page also documents BipHub's zero-analytics posture, satisfying FOUN-05 by absence-of-trackers per D-01/D-02 (no consent banner is needed because no non-essential cookies are set).

Purpose: Make the privacy-rights surface required by GDPR Art 15–17 accessible from every page. Required before public launch.

Output: New static RSC at `app/(public)/privacy/page.tsx` with sections covering Data Controller, What we collect, Legal basis, Retention, Your rights, How to exercise, Children, Updates. Footer.tsx gains a `/privacy` link in its existing "Project" column.
</objective>

<execution_context>
@C:/dev/Antigravity/BIP_project/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/BIP_project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish-static-content-performance-hardening/04-CONTEXT.md
@components/home/Footer.tsx
@components/home/Eyebrow.tsx

<interfaces>
<!-- Storage surfaces that the privacy page MUST enumerate (D-03 + Specifics) -->

1. Supabase Auth cookies: HTTP-only session cookies set by @supabase/ssr on login. Essential (ePrivacy strictly-necessary exemption). Source: lib/supabase/server.ts, lib/supabase/middleware.ts.
2. localStorage key `biphub:bookmarks`: JSON array of BIP slugs, owner-device-only. Set by Zustand bookmarks store (Plan 01-05). Essential (owner-only feature).
3. localStorage key for `bip-draft` Zustand backup: per-session draft auto-save (Plan 02-06). Essential (data-loss prevention), persists until submit or explicit clear.
4. `bips` table content: submitted by coordinators; institutional info (titles, descriptions, contact name + email) — PII for the coordinator only.
5. `profiles` table: full name, university affiliation, contact email, Erasmus code.
6. NO analytics: no Plausible, no GA, no Vercel Analytics, no Umami. NO marketing pixels.

components/home/Footer.tsx existing structure: 4-column grid with "For Students", "For Universities", "Project" columns. The Project column currently has only a GitHub link. Add `/privacy` link there.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create /privacy static RSC with 8 sections covering GDPR rights + zero-analytics posture</name>
  <files>app/(public)/privacy/page.tsx</files>
  <read_first>
    - app/(public)/what-is-a-bip/page.tsx if it exists from Plan 04-01 (match section pattern, container width, heading sizes) — if 04-01 has not run yet, match the pattern in components/home/HowItWorks.tsx
    - components/home/Eyebrow.tsx (eyebrow label pattern)
    - components/home/Footer.tsx (verify the disclaimer + identify where the /privacy link will be added in Task 2)
    - .planning/phases/04-polish-static-content-performance-hardening/04-CONTEXT.md (D-01, D-02, D-03 — locked structure and word count, storage enumeration in specifics block)
    - CLAUDE.md (visual constraints, Footer disclaimer mandate)
  </read_first>
  <action>
    Create `app/(public)/privacy/page.tsx` as a Server Component per D-03.

    Top of file:
    ```tsx
    import type { Metadata } from 'next'
    import { Eyebrow } from '@/components/home/Eyebrow'

    export const dynamic = 'force-static'

    export const metadata: Metadata = {
      title: 'Privacy policy · BipHub',
      description: 'How BipHub processes personal data for EU users. We use no analytics, no third-party trackers, and no marketing pixels. Essential session cookies only.',
      alternates: { canonical: 'https://biphub.eu/privacy' },
    }
    ```

    Layout shell: `<div className="container mx-auto max-w-[800px] px-4 lg:px-6 py-16 lg:py-24">`. Single column (no jump-link sidebar — privacy policies are read top-to-bottom). Each section: `<Eyebrow>` then `<h2 className="text-[clamp(24px,3vw,32px)] font-bold tracking-tight text-ink mt-12 first:mt-0">`, then `<div className="mt-4 text-ink-2 leading-relaxed space-y-4">`.

    Page header: top of page renders an `<h1 className="text-[clamp(32px,4vw,44px)] font-bold tracking-tight text-ink">Privacy policy</h1>` followed by a small "Last updated: 2026-05-13" note in `text-sm text-muted` (use today's date literal — no dynamic date computation since this is force-static).

    Content sections in this EXACT order (D-03), each with the listed prose (Claude drafts; founder review allowed but defaults to ship):

    1. **Data Controller** — "Hexona Systems operates BipHub as an independent open-source project. The data controller for personal data processed through this site is Hexona Systems. For privacy questions or to exercise your rights under GDPR Articles 15–17, contact us at team@hexonasystems.com." (~60 words.)

    2. **What we collect** — Enumerate EVERY storage surface explicitly (Specifics block lock):
       - "Supabase Auth session cookies (essential). When you sign in as a university coordinator, our authentication provider (Supabase) sets HTTP-only cookies that keep you signed in across page loads. These cookies are strictly necessary for the sign-in feature to function and are exempt from consent under EU ePrivacy rules."
       - "Local browser storage. We use your browser's `localStorage` for two strictly-functional purposes: (a) the `biphub:bookmarks` key remembers which BIPs you have bookmarked from the heart icon — this data never leaves your device; (b) the `bip-draft` key holds an in-progress BIP submission so you do not lose your work if your session expires mid-form. Both are essential to the features they support and remain on your device only."
       - "Coordinator profile and submission content. When a university coordinator registers, we store their full name, contact email, university affiliation, and Erasmus institutional code. When they submit a BIP, we store the submission content (programme title, description, dates, contact details they wish to publish, and so on). Approved submissions are published as part of the public Erasmus+ directory."
       - "No analytics. We run no analytics scripts, no third-party trackers, no marketing pixels, no advertising cookies. We do not measure your behaviour. This is by design — the cheapest GDPR-compliant path is to collect nothing." (~280 words for this section.)

    3. **Legal basis** — "For coordinator accounts, the legal basis is contract performance (Art 6(1)(b) GDPR) — we cannot operate the directory without storing the account. For published BIP submissions, the legal basis is legitimate interest (Art 6(1)(f) GDPR) in maintaining a public Erasmus+ directory benefiting students across Europe. We do not rely on consent for any data processing in v1 because we collect no consent-requiring data." (~80 words.)

    4. **Retention** — "Account data is retained until you delete your account from `/dashboard/settings`. When you delete your account, drafts and pending/rejected submissions are deleted; approved BIPs are anonymized (contact name and email are removed) and remain in the public directory as institutional information. Session cookies expire when you sign out or when their issuer's policy expires them." (~70 words.)

    5. **Your rights** — "Under GDPR you have the right of access (Art 15) to a copy of your personal data, the right of rectification (Art 16) to correct inaccurate data, and the right of erasure (Art 17) to have your data deleted. The right of erasure is exercised in-product via the Delete account button at `/dashboard/settings`. For access or rectification requests, email team@hexonasystems.com — we respond within 30 days." (~80 words.)

    6. **How to exercise your rights** — "In-product: open `/dashboard/settings` while signed in and use the Danger zone — Delete account. By email: write to team@hexonasystems.com from the email address on your account. We may ask for additional information to verify your identity before acting on a request affecting personal data." (~60 words.)

    7. **Children** — "BipHub is designed for higher-education students enrolled in Erasmus+-participating institutions. We do not knowingly process the personal data of children under 16 and we do not target children in any of our content." (~40 words.)

    8. **Updates** — "We may update this policy as the product evolves. Material changes will be reflected on this page with an updated date stamp at the top. We do not currently maintain a public change log; if you need to see past versions, write to team@hexonasystems.com." (~50 words.)

    Total target: 600–900 words (D-03 lock). After writing, run a manual word count and trim or expand sections to hit this range.

    Hard constraints:
    - NO `'use client'` — full RSC.
    - NO embedded legal-template boilerplate; the prose above is the deliverable.
    - Contact email is exactly `team@hexonasystems.com` (D-03 + Specifics: matches the configured ADMIN_NOTIFICATION_EMAIL fallback).
    - NO link to a consent banner setting (there is no consent banner — D-02 ).
    - Tailwind class names literal only.

    Implements: FOUN-06 (privacy policy page) and documents the FOUN-05 posture (zero analytics, no consent banner needed) per D-01 / D-02 / D-03.
  </action>
  <acceptance_criteria>
    - File `app/(public)/privacy/page.tsx` exists
    - `grep -q "export const dynamic = 'force-static'" "app/(public)/privacy/page.tsx"` exits 0
    - `grep -q "Privacy policy" "app/(public)/privacy/page.tsx"` exits 0
    - `grep -q "Data Controller\|Data controller" "app/(public)/privacy/page.tsx"` exits 0
    - `grep -q "team@hexonasystems.com" "app/(public)/privacy/page.tsx"` exits 0
    - `grep -q "biphub:bookmarks" "app/(public)/privacy/page.tsx"` exits 0
    - `grep -q "bip-draft" "app/(public)/privacy/page.tsx"` exits 0
    - `grep -q "Supabase Auth" "app/(public)/privacy/page.tsx"` exits 0
    - `grep -q "no analytics\|No analytics" "app/(public)/privacy/page.tsx"` exits 0
    - `grep -q "Article 15\|Art 15" "app/(public)/privacy/page.tsx"` exits 0
    - `grep -q "Article 17\|Art 17" "app/(public)/privacy/page.tsx"` exits 0
    - `grep -q "/dashboard/settings" "app/(public)/privacy/page.tsx"` exits 0
    - Word count: `wc -w "app/(public)/privacy/page.tsx"` body content (after stripping JSX markup) yields between 600 and 900 words — manual verification
    - `grep -v '^//\|^ \*\|^/\*' "app/(public)/privacy/page.tsx" | grep -c "'use client'"` returns 0
    - `npm run build` exits 0 with `/privacy` listed as a static route
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | grep -E "/privacy"</automated>
  </verify>
  <done>
    /privacy renders as a static route with 8 content sections, enumerates every storage surface, names team@hexonasystems.com as data controller contact, and documents the zero-analytics posture.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add /privacy link to public Footer Project column</name>
  <files>components/home/Footer.tsx</files>
  <read_first>
    - components/home/Footer.tsx (full file — current state: verify the existing /what-is-a-bip link is already present in the navigation list (grep confirms it at line ~38); confirm /privacy does NOT already exist anywhere in the file before adding it; must preserve existing INFO-03 disclaimer EXACTLY: "Independent project — not affiliated with the European Commission" with em-dash and no period; must preserve existing /what-is-a-bip + /bips + /register + /login + GitHub links; must preserve existing 4-column grid structure)
    - CLAUDE.md (Footer disclaimer mandate — every page MUST show this disclaimer)
  </read_first>
  <action>
    Modify `components/home/Footer.tsx` to add a `/privacy` link inside the existing "Project" column.

    The Project column currently contains a `<ul className="mt-4 space-y-2 text-sm">` with a single `<li>` for the GitHub link. Add a NEW `<li>` BEFORE the GitHub `<li>` with:

    ```tsx
    <li><Link href="/privacy" className="text-white/80 transition-colors hover:text-eu-gold">Privacy policy</Link></li>
    ```

    The Link uses `next/link` — `Link` is already imported at the top of the file. Do NOT reorder any other list items. Do NOT modify the bottom-row disclaimer block. Do NOT modify the brand column or the For Students / For Universities columns.

    DO NOT touch the existing INFO-03 disclaimer line:
    `<p className="text-xs text-white/60">Independent project — not affiliated with the European Commission</p>`
    The em-dash and lack of trailing period are load-bearing — Phase 1 Plan 01-04 locked this string.

    Implements: link surface for FOUN-06 — the privacy page is now reachable from every public-route page via the footer.
  </action>
  <acceptance_criteria>
    - `grep -q 'href="/privacy"' components/home/Footer.tsx` exits 0
    - `grep -c "/privacy" components/home/Footer.tsx` returns exactly 1 (not zero — confirms link was added; not two — prevents duplicate addition)
    - `grep -q "Privacy policy" components/home/Footer.tsx` exits 0
    - `grep -q "Independent project — not affiliated with the European Commission" components/home/Footer.tsx` exits 0 (em-dash preserved)
    - `grep -q "/what-is-a-bip" components/home/Footer.tsx` exits 0 (existing link preserved)
    - `grep -q "github.com/biphub/biphub" components/home/Footer.tsx` exits 0 (existing GitHub link preserved)
    - `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'href="/privacy"' components/home/Footer.tsx &amp;&amp; grep -q "Independent project — not affiliated with the European Commission" components/home/Footer.tsx</automated>
  </verify>
  <done>
    Footer Project column lists Privacy policy above GitHub. INFO-03 disclaimer preserved verbatim.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → /privacy | Public read-only route; no input crosses any boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | I (Information disclosure) | Privacy policy completeness | mitigate | Page enumerates every storage surface listed in CONTEXT.md Specifics; missing one would breach GDPR Art 13 transparency obligation |
| T-04-05 | R (Repudiation) | Stale "last updated" date | accept | Date is a literal string; future policy edits require manually bumping the date; v1 has no audit-log of policy edits (deferred) |
| T-04-06 | T (Tampering) | Footer INFO-03 disclaimer modification | mitigate | Acceptance criteria includes grep for the exact disclaimer string; CLAUDE.md never-do prohibits removal |
</threat_model>

<verification>
- `npm run build` succeeds; `/privacy` shown as static route
- Manual: opening `/privacy` renders 8 sections, dev server shows no console errors
- Manual: opening any public route (`/`, `/bips`, `/bip/[slug]`) shows the new Privacy policy link in the footer alongside the existing GitHub link
- Manual: INFO-03 disclaimer still rendered at footer bottom
</verification>

<success_criteria>
- FOUN-06 satisfied: privacy policy page exists, linked from footer on every public page
- FOUN-05 satisfied by absence: no consent banner shipped because no consent-requiring scripts run; privacy page documents this posture
- All 5 essential storage surfaces enumerated in the "What we collect" section
- GDPR Art 15–17 rights documented and the in-product erasure path (/dashboard/settings) named explicitly
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-static-content-performance-hardening/04-02-SUMMARY.md`
</output>
