---
phase: 04-polish-static-content-performance-hardening
plan: 06
type: execute
wave: 3
depends_on: ["04-05"]
files_modified:
  - next.config.ts
  - package.json
  - app/(public)/bips/page.tsx
  - components/bip/BipFiltersSidebarSkeleton.tsx
  - components/bip/BipSearchBarSkeleton.tsx
  - components/bip/BipSortControlSkeleton.tsx
  - components/bip/BipPaginationSkeleton.tsx
autonomous: false
requirements: []
must_haves:
  truths:
    - "Every useSearchParams() consumer is wrapped in a Suspense boundary with a stationary skeleton fallback"
    - "Running `npm run build:analyze` produces the @next/bundle-analyzer HTML report"
    - "All <img> tags in the codebase have been audited; none remain (or each documented exception is justified in a code comment)"
    - "Manual Lighthouse audits captured as screenshots for /, /bips, /bip/[slug], /what-is-a-bip — all four pages score ≥90 Performance/Accessibility/SEO with LCP <1.5s mobile 4G"
  artifacts:
    - path: "next.config.ts"
      provides: "next config wrapped in withBundleAnalyzer behind ANALYZE=true env var"
      contains: "withBundleAnalyzer"
    - path: "package.json"
      provides: "Three new scripts (test:e2e, test:e2e:ui, build:analyze) and @next/bundle-analyzer devDep"
      contains: "build:analyze"
    - path: "app/(public)/bips/page.tsx"
      provides: "Suspense-wrapped filter/search/sort/pagination components"
      contains: "<Suspense"
    - path: "components/bip/BipFiltersSidebarSkeleton.tsx"
      provides: "Stationary skeleton fallback (no CLS) matching sidebar dimensions"
    - path: ".planning/phases/04-polish-static-content-performance-hardening/lighthouse/"
      provides: "Manual Lighthouse audit screenshots for 4 routes"
  key_links:
    - from: "app/(public)/bips/page.tsx"
      to: "BipFiltersSidebarSkeleton et al"
      via: "Suspense fallback prop"
      pattern: "<Suspense.*fallback"
    - from: "next.config.ts"
      to: "@next/bundle-analyzer"
      via: "withBundleAnalyzer wrapper"
      pattern: "withBundleAnalyzer"
---

<objective>
Ship performance hardening (D-18 Suspense boundaries, D-19 bundle analyzer, D-20 manual Lighthouse audits, D-21 image audit) plus the package.json script additions (D-24). The phase goal of "Lighthouse > 90 on Performance/Accessibility/SEO; LCP < 1.5s on 4G mobile" is verified here.

Note: This plan satisfies SC-4 (Lighthouse > 90) and CONTEXT D-18..D-21 — it has no Phase 4 REQ-ID mapping. FOUN-02 was completed in Phase 1; this plan revalidates that lock as actually-measured. INFO-04 (EC outbound link on /what-is-a-bip) is covered entirely by Plan 04-01; this plan does NOT modify /what-is-a-bip. The Lighthouse audits captured here cover INFO-01/02/04 pages indirectly as part of the four-route audit set. The `requirements:` frontmatter is therefore empty by design.

Sequencing note: this plan is wave 3 with `depends_on: ["04-05"]` to serialize package.json writes across plans 04-05/04-06/04-07. Plan 04-05 itself does not touch package.json, but the depends_on edge enforces serial execution to keep the dependency graph simple and prevent drift if a future revision of 04-05 starts touching package.json.

Purpose: Lock in the Phase 1 FOUN-02 success criteria as actually-measured baselines for v1 launch. The Suspense boundaries also fix a latent CLS issue on `/bips` (filter state resolution shifts layout without them).

Output: next.config.ts wrapped in withBundleAnalyzer, package.json scripts + devDep, Suspense boundaries around 4 useSearchParams consumers + 4 matching stationary skeletons, Lighthouse audit screenshots committed to `.planning/phases/04-.../lighthouse/`.
</objective>

<execution_context>
@C:/dev/Antigravity/BIP_project/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/BIP_project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-polish-static-content-performance-hardening/04-CONTEXT.md
@next.config.ts
@package.json
@app/(public)/bips/page.tsx
@components/bip/BipFiltersSidebar.tsx
@components/bip/BipFiltersDrawer.tsx
@components/bip/BipSearchBar.tsx
@components/bip/BipSortControl.tsx
@components/bip/BipPagination.tsx
@components/home/MapSkeleton.tsx

<interfaces>
<!-- useSearchParams consumers (from D-18) -->
- BipFiltersSidebar (desktop filter sidebar) — Plan 01-06 — uses useSearchParams to read current filter state
- BipFiltersDrawer (mobile filter drawer) — Plan 01-06 — same
- BipSearchBar (search input above grid) — Plan 01-06 — debounced URL update
- BipSortControl (sort dropdown) — Plan 01-06 — URL update on change
- BipPagination (numbered pagination) — Plan 01-06 — URL pages

All five live in /bips. The detail page (/bip/[slug]) has no useSearchParams consumer in v1.

The homepage also gets a Suspense wrap from Plan 04-05 (AccountDeletedToastIsland useSearchParams). DO NOT duplicate that wrap here.

<!-- Existing skeleton pattern (Phase 1 Plan 01-05) -->
components/home/MapSkeleton.tsx — fixed dimensions matching the rendered EuropeMap, no spinner, no animation. Match this visual language.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add @next/bundle-analyzer devDep + three package.json scripts</name>
  <files>package.json</files>
  <read_first>
    - package.json (full file — preserve all existing scripts, deps, devDeps; add ONLY the three new scripts and the one new devDep)
    - .planning/phases/04-polish-static-content-performance-hardening/04-CONTEXT.md (D-19, D-24 — exact script names and behaviour)
  </read_first>
  <action>
    ROADMAP Phase 4 Key Deliverables mentions `db:migrate` — this is OVERRIDDEN by CONTEXT D-24. Do NOT add `db:migrate`; `supabase db push` is canonical. The acceptance criteria below explicitly assert `db:migrate` is absent.

    Modify `package.json` to:

    1. Add THREE new scripts in the `scripts` block:
       ```json
       "test:e2e": "playwright test",
       "test:e2e:ui": "playwright test --ui",
       "build:analyze": "cross-env ANALYZE=true next build"
       ```
       Insert them after the existing `"build"` line (alphabetical-ish grouping). The `cross-env` prefix on `build:analyze` is REQUIRED — Windows shells (PowerShell, cmd) do not support `ANALYZE=true next build` syntax. If `cross-env` is not already a devDep, ALSO add it.

    2. Add `@next/bundle-analyzer` to `devDependencies`:
       ```json
       "@next/bundle-analyzer": "^15.5.0"
       ```
       Pin the major version to match the locked Next.js 15.5 LTS (CLAUDE.md).

    3. Add `cross-env` to `devDependencies` if not already present:
       ```json
       "cross-env": "^7.0.3"
       ```

    4. **Do NOT add `playwright` or `@playwright/test` here** — Plan 04-07 owns the Playwright dep installation. The `test:e2e` and `test:e2e:ui` scripts are added now (so they appear in the scripts list for CONTRIBUTING.md docs from Plan 04-04), but they will only function after Plan 04-07 installs Playwright. This is acceptable — the scripts are no-ops without the binary.

    5. **Do NOT add `db:migrate`** — D-24 explicitly excludes this (supabase db push is canonical).

    After editing the JSON, run `npm install` to install the new devDeps. Verify lockfile updates and that `node_modules/@next/bundle-analyzer` exists. The added devDeps must NOT pull in any peer-deps that conflict with the existing locked versions (Next 15.5.x, React 19.2.x).

    Implements: D-19 + D-24.
  </action>
  <acceptance_criteria>
    - `grep -q '"test:e2e":' package.json` exits 0
    - `grep -q '"test:e2e:ui":' package.json` exits 0
    - `grep -q '"build:analyze":' package.json` exits 0
    - `grep -q '"@next/bundle-analyzer":' package.json` exits 0
    - `grep -q '"cross-env":' package.json` exits 0
    - `grep -q '"db:migrate":' package.json` returns 0 lines (D-24 lock)
    - `npm install` exits 0 (no peer-dep conflicts)
    - `test -d node_modules/@next/bundle-analyzer` exits 0
    - `npx --no-install cross-env --help` exits 0 (cross-env installed)
    - All existing scripts preserved: `grep -q '"dev": "next dev"' package.json && grep -q '"db:reset"' package.json && grep -q '"db:types"' package.json && grep -q '"verify:seed"' package.json && grep -q '"build:topojson"' package.json && grep -q '"fonts:fetch"' package.json` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm install &amp;&amp; test -d node_modules/@next/bundle-analyzer</automated>
  </verify>
  <done>
    package.json contains three new scripts (test:e2e, test:e2e:ui, build:analyze), @next/bundle-analyzer and cross-env in devDeps, no db:migrate script, all prior scripts intact. node_modules installed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wrap next.config.ts in withBundleAnalyzer behind ANALYZE=true</name>
  <files>next.config.ts</files>
  <read_first>
    - next.config.ts (current contents — minimal, only sets reactStrictMode: true)
    - .planning/phases/04-polish-static-content-performance-hardening/04-CONTEXT.md (D-19 — exact toggle)
  </read_first>
  <action>
    Replace `next.config.ts` with:

    ```ts
    import type { NextConfig } from 'next'
    import bundleAnalyzer from '@next/bundle-analyzer'

    const withBundleAnalyzer = bundleAnalyzer({
      enabled: process.env.ANALYZE === 'true',
    })

    const nextConfig: NextConfig = {
      reactStrictMode: true,
    }

    export default withBundleAnalyzer(nextConfig)
    ```

    Hard constraints:
    - The `enabled` flag MUST check exactly `process.env.ANALYZE === 'true'` — NOT `!!process.env.ANALYZE` (which would enable on any non-empty value including "false").
    - Keep `reactStrictMode: true` (Phase 1 lock).
    - DO NOT add any other config keys (no `images.remotePatterns`, no `experimental.*`, no `headers()`). Those are out of scope for v1; introducing them here would expand the surface area without test coverage.
    - The bundle-analyzer ESM/CJS import resolves cleanly with the v15.5 LTS Next.js — the `import bundleAnalyzer from '@next/bundle-analyzer'` default-import works because the package ships a CJS module with `module.exports = function` (works as default import under TS `esModuleInterop`).

    After editing, verify `npm run build` still succeeds in normal mode (no analyzer) AND `npm run build:analyze` opens a browser tab with the bundle analyzer report.
  </action>
  <acceptance_criteria>
    - `grep -q "withBundleAnalyzer" next.config.ts` exits 0
    - `grep -q "process.env.ANALYZE === 'true'" next.config.ts` exits 0
    - `grep -q "reactStrictMode: true" next.config.ts` exits 0
    - `grep -q "export default withBundleAnalyzer(nextConfig)" next.config.ts` exits 0
    - `npm run build` exits 0 (no analyzer, no opening browser)
    - `npm run build:analyze` exits 0 — generates HTML reports in `.next/analyze/` (manual inspection — the script may also open browser tabs; do not block on that)
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    next.config.ts wraps the config in withBundleAnalyzer with an exact-string ANALYZE env check; normal builds unaffected; analyzer builds generate HTML reports.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create 4 stationary skeleton components for /bips Suspense fallbacks</name>
  <files>components/bip/BipFiltersSidebarSkeleton.tsx, components/bip/BipSearchBarSkeleton.tsx, components/bip/BipSortControlSkeleton.tsx, components/bip/BipPaginationSkeleton.tsx</files>
  <read_first>
    - components/bip/BipFiltersSidebar.tsx (read full file — the skeleton MUST match the resolved component's rendered dimensions exactly; measure container widths, gap sizes, number of filter sections)
    - components/bip/BipSearchBar.tsx (input height + width)
    - components/bip/BipSortControl.tsx (dropdown trigger dimensions)
    - components/bip/BipPagination.tsx (button row dimensions)
    - components/home/MapSkeleton.tsx (existing skeleton pattern — fixed dimensions, no animation, no spinner)
    - .planning/phases/04-polish-static-content-performance-hardening/04-CONTEXT.md (Specifics — "Suspense fallback for BipFilters" — stationary skeleton, no spinner)
  </read_first>
  <action>
    Create FOUR new files, each a Server Component (NO `'use client'`) returning a stationary skeleton with the exact rendered dimensions of the live component.

    **File 1: `components/bip/BipFiltersSidebarSkeleton.tsx`**
    ```tsx
    /**
     * Stationary skeleton for BipFiltersSidebar Suspense fallback (D-18).
     * Matches the live sidebar's dimensions exactly so the Suspense → resolved
     * transition causes no Cumulative Layout Shift (CLS).
     */
    export function BipFiltersSidebarSkeleton() {
      return (
        <div
          aria-hidden
          className="w-full space-y-6 rounded-lg border border-border bg-bg-soft p-4"
        >
          {/* 6 filter section placeholders matching the live sidebar's accordion entries */}
          {[0, 1, 2, 3, 4, 5].map((i) => (
            <div key={i} className="space-y-2">
              <div className="h-4 w-32 rounded bg-border" />
              <div className="h-8 w-full rounded bg-border/60" />
            </div>
          ))}
        </div>
      )
    }
    ```

    **File 2: `components/bip/BipSearchBarSkeleton.tsx`**
    ```tsx
    /** Stationary skeleton for BipSearchBar (D-18). */
    export function BipSearchBarSkeleton() {
      return (
        <div aria-hidden className="h-10 w-full max-w-md rounded-lg bg-border/60" />
      )
    }
    ```

    **File 3: `components/bip/BipSortControlSkeleton.tsx`**
    ```tsx
    /** Stationary skeleton for BipSortControl (D-18). */
    export function BipSortControlSkeleton() {
      return (
        <div aria-hidden className="h-9 w-40 rounded-md bg-border/60" />
      )
    }
    ```

    **File 4: `components/bip/BipPaginationSkeleton.tsx`**
    ```tsx
    /** Stationary skeleton for BipPagination (D-18). */
    export function BipPaginationSkeleton() {
      return (
        <div aria-hidden className="flex h-10 items-center justify-center gap-2">
          {[0, 1, 2, 3, 4].map((i) => (
            <div key={i} className="h-9 w-9 rounded bg-border/60" />
          ))}
        </div>
      )
    }
    ```

    Hard constraints:
    - NO `animate-pulse` or any animation — the Specifics block locks "stationary skeleton, do NOT use a spinner (CLS penalty if it shifts layout when filters resolve)".
    - NO `'use client'` — skeletons are RSC.
    - `aria-hidden` on the outer div — skeletons are visual placeholders, not announce-worthy.
    - Tailwind class names literal only.
    - The sidebar skeleton MUST roughly match the live BipFiltersSidebar height — if the live sidebar has 6 accordion sections, the skeleton renders 6 placeholders. Read BipFiltersSidebar.tsx in this task's read_first list to confirm the count and adjust the loop if needed.
  </action>
  <acceptance_criteria>
    - `test -f components/bip/BipFiltersSidebarSkeleton.tsx` exits 0
    - `test -f components/bip/BipSearchBarSkeleton.tsx` exits 0
    - `test -f components/bip/BipSortControlSkeleton.tsx` exits 0
    - `test -f components/bip/BipPaginationSkeleton.tsx` exits 0
    - `grep -L "animate-pulse" components/bip/*Skeleton.tsx` lists all 4 files (none contain animate-pulse)
    - `grep -L "'use client'" components/bip/*Skeleton.tsx` lists all 4 files
    - `grep -c 'aria-hidden' components/bip/BipFiltersSidebarSkeleton.tsx components/bip/BipSearchBarSkeleton.tsx components/bip/BipSortControlSkeleton.tsx components/bip/BipPaginationSkeleton.tsx` reports ≥1 in each file
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    Four stationary skeletons exist, all RSC, no animation, dimensioned to match the live components.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Wrap useSearchParams consumers in /bips/page.tsx with Suspense + skeleton fallbacks</name>
  <files>app/(public)/bips/page.tsx</files>
  <read_first>
    - app/(public)/bips/page.tsx (full file — identify the rendered JSX positions of BipFiltersSidebar, BipFiltersDrawer, BipSearchBar, BipSortControl, BipPagination)
    - components/bip/BipFiltersSidebar.tsx (confirm it uses useSearchParams — if so it needs the wrap)
    - components/bip/BipFiltersDrawer.tsx (same check)
    - components/bip/BipSearchBar.tsx (same)
    - components/bip/BipSortControl.tsx (same)
    - components/bip/BipPagination.tsx (same)
    - .planning/phases/04-polish-static-content-performance-hardening/04-CONTEXT.md (D-18 — audit every useSearchParams consumer)
  </read_first>
  <action>
    Modify `app/(public)/bips/page.tsx` to wrap each useSearchParams-consuming child component in a `<Suspense>` boundary with the corresponding skeleton fallback.

    Add imports at top:
    ```tsx
    import { Suspense } from 'react'
    import { BipFiltersSidebarSkeleton } from '@/components/bip/BipFiltersSidebarSkeleton'
    import { BipSearchBarSkeleton } from '@/components/bip/BipSearchBarSkeleton'
    import { BipSortControlSkeleton } from '@/components/bip/BipSortControlSkeleton'
    import { BipPaginationSkeleton } from '@/components/bip/BipPaginationSkeleton'
    ```

    For each useSearchParams consumer identified in the read_first audit, wrap the JSX usage:

    Before:
    ```tsx
    <BipFiltersSidebar filters={filters} />
    ```
    After:
    ```tsx
    <Suspense fallback={<BipFiltersSidebarSkeleton />}>
      <BipFiltersSidebar filters={filters} />
    </Suspense>
    ```

    Apply the same pattern to BipFiltersDrawer (reuse `BipFiltersSidebarSkeleton` as fallback — the drawer is mobile-only and the sidebar skeleton's height is acceptable in the drawer container), BipSearchBar, BipSortControl, BipPagination.

    For BipFilterChips (the chip row above the grid) — if it does NOT use useSearchParams, do not wrap. Audit the file in read_first; if it does, ALSO wrap it with a one-line skeleton like `<div aria-hidden className="h-6 w-full" />`.

    Hard constraints:
    - One Suspense boundary per consumer (NOT a single boundary wrapping the whole page). Per-consumer boundaries minimize the visual area that goes blank during navigation.
    - Fallbacks MUST be the skeleton components from Task 3 — not `null`, not `<div />`, not a spinner.
    - DO NOT move the `parseSearchParams` / `getBips` data fetching — those are RSC-level and resolve before render; only the client components need wrapping.
    - DO NOT touch the `generateMetadata` function or the `export const revalidate = 3600` constant.
    - If a component does NOT use useSearchParams (e.g., the BipGrid which receives `rows` as a prop), DO NOT wrap it — pointless Suspense overhead.
  </action>
  <acceptance_criteria>
    - `grep -c "<Suspense" "app/(public)/bips/page.tsx"` returns ≥ 4 (one per wrapped consumer)
    - `grep -q "BipFiltersSidebarSkeleton" "app/(public)/bips/page.tsx"` exits 0
    - `grep -q "BipSearchBarSkeleton" "app/(public)/bips/page.tsx"` exits 0
    - `grep -q "BipSortControlSkeleton" "app/(public)/bips/page.tsx"` exits 0
    - `grep -q "BipPaginationSkeleton" "app/(public)/bips/page.tsx"` exits 0
    - `grep -q "fallback={<.*Skeleton" "app/(public)/bips/page.tsx"` exits 0
    - `npm run build` exits 0 with `/bips` listed
    - Manual: navigate to /bips, throttle network to slow 3G via DevTools, refresh — skeletons render briefly without layout shift before live components hydrate
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&amp;1 | grep -E "/bips" &amp;&amp; grep -c "&lt;Suspense" "app/(public)/bips/page.tsx" | awk '{ if ($1 &gt;= 4) exit 0; else exit 1 }'</automated>
  </verify>
  <done>
    Every useSearchParams consumer on /bips is wrapped in a Suspense boundary with a stationary skeleton fallback; no layout shift on hydration.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Image audit — sweep for raw <img> tags, migrate to next/image or document justified exceptions</name>
  <files>(audit-only — modifications as found)</files>
  <read_first>
    - app/ and components/ directories (search for `<img` tags using grep; for each match decide: migrate or justify)
    - public/ directory listing (verify the assets referenced by any found <img> tags)
    - .planning/phases/04-polish-static-content-performance-hardening/04-CONTEXT.md (D-21 — sweep, migrate, hero LCP candidates get priority)
  </read_first>
  <action>
    Run a repository sweep:
    ```
    grep -rn "<img " app/ components/ --include="*.tsx" --include="*.jsx"
    ```

    For EACH match:
    1. **If the image is decorative or static (e.g., a /public asset)**: migrate to `next/image`:
       ```tsx
       import Image from 'next/image'
       import heroImg from '@/public/hero.png'  // static import gives width/height automatically
       // ...
       <Image src={heroImg} alt="..." priority /> // priority on LCP candidates only
       ```
       Add `priority` attribute on at most ONE image per route — the hero / LCP candidate. Adding it to every image defeats the purpose.

    2. **If the image is dynamic (from a remote URL not in /public)**: migrate to `next/image` with explicit width/height:
       ```tsx
       <Image src={dynamicUrl} alt="..." width={W} height={H} />
       ```
       AND add the host to `next.config.ts` `images.remotePatterns` — but BEFORE doing this, verify the use case justifies the next.config surface area expansion. For v1 there should be NO dynamic image hosts (BIP submission does not include images per CONTEXT). If a dynamic image use case is found, FLAG it in the plan SUMMARY for human review rather than silently adding remotePatterns.

    3. **If the image is intentionally raw (e.g., inside a generateMetadata return that creates static OG metadata, OR inside an opengraph-image.tsx Edge-rendered handler)**: leave it and add a one-line code comment explaining why:
       ```tsx
       {/* eslint-disable-next-line @next/next/no-img-element -- OG image handler runs on Edge; next/image not available */}
       <img src="..." alt="..." />
       ```

    4. **If the audit finds ZERO `<img>` tags**: that is the expected outcome for the current codebase (Phase 1/2/3 used next/image throughout). Document this in the plan SUMMARY: "Image audit: 0 raw <img> tags found; no migration needed."

    Specifically AUDIT but DO NOT modify:
    - `scripts/og-template.html` from Plan 04-03 — that file is HTML, not JSX; `<img>` is correct there if any are present
    - `app/(public)/bip/[slug]/opengraph-image.tsx` from Phase 1 Plan 01-07 — Edge runtime, next/image not available
    - Any `.md` files — Markdown img syntax is unaffected

    Hard constraints:
    - DO NOT add `images.remotePatterns` to next.config.ts unless a real dynamic image use case is found. For v1, the expected result is "audit clean".
    - The `priority` flag MUST appear at most once per route — adding it to every image is the most common Lighthouse-counterproductive change.
    - DO NOT replace working `<Image>` (capitalized) components — those are already migrated.

    Implements: D-21.
  </action>
  <acceptance_criteria>
    - `grep -rn "<img " app/ components/ --include="*.tsx" --include="*.jsx"` returns 0 matches OR every match has a `eslint-disable.*no-img-element` comment with a justification on the line above
    - If migrations happened, `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
    - Plan SUMMARY records the audit result (zero matches expected for current codebase)
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    Codebase has zero unjustified raw <img> tags. Migrations (if any) committed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Manual Lighthouse audits — capture screenshots for / and /bips and /bip/[slug] and /what-is-a-bip</name>
  <read_first>
    - .planning/phases/04-polish-static-content-performance-hardening/04-CONTEXT.md (D-20 — page list, target metrics, screenshot location)
    - app/(public)/what-is-a-bip/page.tsx (Plan 04-01 output — verify it exists before auditing it)
  </read_first>
  <what-built>
    Tasks 1–5 deliver the runtime improvements (Suspense, image audit, bundle analyzer wiring). Lighthouse measures the result. Lighthouse CI is explicitly OUT of scope for v1 (D-20: manual snapshots only); this checkpoint captures a manual baseline.
  </what-built>
  <how-to-verify>
    1. Ensure local Supabase is running (`supabase status` shows services up) and the database is seeded (`npm run db:reset` if not).
    2. Build for production: `npm run build`.
    3. Start production server: `npm start`. Wait for "ready in ..." log.
    4. Open Chrome (NOT Edge — Chrome's Lighthouse panel is the reference implementation).
    5. Open DevTools → Lighthouse panel.
    6. Configure for each audit:
       - Mode: Navigation (default)
       - Device: Mobile
       - Categories: Performance, Accessibility, Best Practices, SEO (all 4 checked)
       - Plugins: none
    7. Run audit for each of the 4 routes in turn:
       - http://localhost:3000/
       - http://localhost:3000/bips
       - http://localhost:3000/bip/{any-slug-from-seed} (pick one — e.g., the first slug listed in the homepage Recently Added section)
       - http://localhost:3000/what-is-a-bip
    8. For each audit:
       - Click "Generate report"
       - Capture a FULL-PAGE screenshot of the resulting score panel (showing the four circular scores + the LCP / FCP / CLS / TBT metrics)
       - Save the screenshot to `.planning/phases/04-polish-static-content-performance-hardening/lighthouse/{route-slug}.png`
         - `/` → `lighthouse/home.png`
         - `/bips` → `lighthouse/bips.png`
         - `/bip/{slug}` → `lighthouse/bip-detail.png`
         - `/what-is-a-bip` → `lighthouse/what-is-a-bip.png`
    9. Verify each report meets the locked targets from FOUN-02 / D-20:
       - Performance ≥ 90
       - Accessibility ≥ 90
       - SEO ≥ 90
       - LCP < 1.5s
    10. If any score is below target, INVESTIGATE before approving the checkpoint — do not paper over a fail. Document findings in the plan SUMMARY.

    Note: Lighthouse mobile audits include "Simulated throttling" to 4G by default — that satisfies the "4G mobile" clause in FOUN-02. Do NOT switch to "Applied throttling" (it produces less stable scores in v1).
  </how-to-verify>
  <acceptance_criteria>
    - `test -d .planning/phases/04-polish-static-content-performance-hardening/lighthouse` exits 0
    - `test -f .planning/phases/04-polish-static-content-performance-hardening/lighthouse/home.png` exits 0
    - `test -f .planning/phases/04-polish-static-content-performance-hardening/lighthouse/bips.png` exits 0
    - `test -f .planning/phases/04-polish-static-content-performance-hardening/lighthouse/bip-detail.png` exits 0
    - `test -f .planning/phases/04-polish-static-content-performance-hardening/lighthouse/what-is-a-bip.png` exits 0
    - All 4 screenshots show Performance ≥ 90, Accessibility ≥ 90, SEO ≥ 90, LCP < 1.5s
    - Any score below target is investigated and documented in the SUMMARY before the checkpoint is approved
  </acceptance_criteria>
  <resume-signal>Type "approved" once all 4 audits show ≥90 scores and LCP <1.5s and the screenshots are committed; or describe failing scores and proposed remediation.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lighthouse audit → public routes | Audits read-only; no boundary breach |
| Bundle analyzer report → developer browser | Local-only file:// URL; not exposed externally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-22 | D (Denial of service) | Lighthouse score drift after future PRs | accept | Manual baseline only in v1; Lighthouse CI deferred to v1.1 per D-20. PR checklist in CONTRIBUTING.md reminds contributors to re-run before perf-flag PRs. |
| T-04-23 | I (Information disclosure) | Bundle analyzer report committed to repo by accident | mitigate | `.next/analyze/` is gitignored by default in the Next.js .gitignore template; verify the repo's .gitignore covers this (audit during execution) |
| T-04-24 | T (Tampering) | Suspense boundaries cause hydration mismatch | mitigate | Skeletons are RSC-rendered identically server and client; fallback markup matches what Suspense produces during streaming |
</threat_model>

<verification>
- `npm run build` succeeds with no Suspense warnings
- `npm run build:analyze` opens the bundle analyzer report (manual verify)
- Lighthouse screenshots committed for all 4 routes meeting targets
- Image audit documented (expected: zero raw <img> tags)
</verification>

<success_criteria>
- D-18 satisfied: every useSearchParams consumer on /bips wrapped in a Suspense boundary with stationary skeleton
- D-19 satisfied: bundle analyzer wired behind ANALYZE=true; `npm run build:analyze` works
- D-20 satisfied: 4 manual Lighthouse screenshots committed; all 4 routes ≥90 Perf/A11y/SEO and LCP <1.5s
- D-21 satisfied: image audit complete; no raw <img> tags remain
- D-24 satisfied: package.json has test:e2e, test:e2e:ui, build:analyze; no db:migrate
- FOUN-02 (locked Phase 1) revalidated as actually-measured for the four current public routes
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-static-content-performance-hardening/04-06-SUMMARY.md`
</output>
