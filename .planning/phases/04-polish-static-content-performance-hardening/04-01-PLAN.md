---
phase: 04-polish-static-content-performance-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(public)/what-is-a-bip/page.tsx
autonomous: true
requirements: [INFO-01, INFO-02, INFO-04]
must_haves:
  truths:
    - "A student can navigate to /what-is-a-bip from the existing footer link"
    - "The page renders 5 content sections plus an 8-item FAQ accordion"
    - "The page contains an outbound link to the official EC Erasmus+ BIP programme guide"
    - "The page is statically rendered (force-static) and indexable"
  artifacts:
    - path: "app/(public)/what-is-a-bip/page.tsx"
      provides: "Static RSC explainer page with FAQ"
      contains: "export const dynamic = 'force-static'"
  key_links:
    - from: "components/home/Footer.tsx"
      to: "/what-is-a-bip"
      via: "existing Link href (already shipped Plan 01-04)"
      pattern: "href=\"/what-is-a-bip\""
---

<objective>
Ship the `/what-is-a-bip` static explainer page satisfying INFO-01 (BIP explainer covering KA131 / virtual component / ECTS / eligibility), INFO-02 (FAQ section), and INFO-04 (outbound link to official EC documentation).

Purpose: Students arriving from the footer "What is a BIP?" link currently 404 — the existing footer link (Plan 01-04) was forward-declared. This plan delivers the destination.

Output: Single static RSC at `app/(public)/what-is-a-bip/page.tsx` with `force-static` revalidation, 5 content sections, an 8-item FAQ accordion (locked list from D-06), and a verified outbound link to the EC programme guide.
</objective>

<execution_context>
@C:/dev/Antigravity/BIP_project/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/BIP_project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish-static-content-performance-hardening/04-CONTEXT.md
@app/(public)/layout.tsx
@components/home/Footer.tsx
@components/home/Eyebrow.tsx

<interfaces>
<!-- Existing chrome that consumes this route -->

components/home/Footer.tsx ALREADY contains:
```tsx
<li><Link href="/what-is-a-bip" className="text-white/80 transition-colors hover:text-eu-gold">What is a BIP?</Link></li>
```
Footer link target is /what-is-a-bip. Do NOT modify Footer.tsx — the link was pre-shipped in Plan 01-04.

app/(public)/layout.tsx wraps this route with StickyNav + Footer + Toaster + skip-link.

components/home/Eyebrow.tsx exists for uppercase blue eyebrow labels with gold leading dash (matches existing section pattern).

components/ui/accordion.tsx exists from Phase 1 Plan 01-06 (BipFiltersSidebar uses @base-ui/react Accordion with `multiple` prop, NOT `type='multiple'`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create /what-is-a-bip static RSC with 5 sections + 8-item FAQ + EC outbound link</name>
  <files>app/(public)/what-is-a-bip/page.tsx</files>
  <read_first>
    - app/(public)/page.tsx (homepage — to match section padding + eyebrow voice + clamp() heading sizes used by Hero/HowItWorks)
    - components/home/HowItWorks.tsx (reference for section composition, eyebrow + h2 + body pattern)
    - components/home/Eyebrow.tsx (uppercase blue eyebrow with gold leading dash — used at top of each section)
    - components/ui/accordion.tsx (existing shadcn/@base-ui Accordion — confirm import path and prop API; Phase 1 uses `multiple` prop)
    - components/home/Footer.tsx (verify the link exists at /what-is-a-bip — DO NOT modify)
    - CLAUDE.md (visual constraints — 96px section padding desktop, EU palette, Inter font, clamp() heading sizes, no EU 12-star emblem)
    - .planning/phases/04-polish-static-content-performance-hardening/04-CONTEXT.md (D-04, D-05, D-06 — locked structure, copy authorship, FAQ list)
  </read_first>
  <action>
    Create `app/(public)/what-is-a-bip/page.tsx` as a Server Component (no 'use client') per D-04.

    Top of file:
    ```tsx
    import type { Metadata } from 'next'
    import Link from 'next/link'
    import { Eyebrow } from '@/components/home/Eyebrow'
    import { Accordion, AccordionItem, AccordionTrigger, AccordionContent } from '@/components/ui/accordion'

    export const dynamic = 'force-static'

    export const metadata: Metadata = {
      title: 'What is a BIP? · BipHub',
      description: 'An Erasmus+ Blended Intensive Programme (BIP) is a short, intensive course combining a 5–10 day in-person mobility with a virtual component, worth 3–6 ECTS credits. Here is what to know before applying.',
      alternates: { canonical: 'https://biphub.eu/what-is-a-bip' },
    }
    ```

    The page MUST contain exactly these 5 content sections in this order (D-04), each with an `id` matching the jump-link target so the desktop sidebar nav anchors work:

    1. `id="what"` — **What is a BIP?**: Define BIP = Blended Intensive Programme. Erasmus+ KA131 context (the EU's flagship higher-education mobility scheme). "Intensive" = 5–10 day in-person component. "Blended" = paired virtual learning component (before, during, or after). 3–4 paragraphs of plainspoken prose, no jargon-stacking (D-05 voice).

    2. `id="virtual-physical"` — **Virtual + physical components**: The 5–10 day physical mobility requirement (typically held at the host university). The virtual component (online lectures, group work, asynchronous materials) is mandatory — it is what makes a BIP "blended" rather than a normal short exchange. 2–3 paragraphs.

    3. `id="ects"` — **ECTS, eligibility, language**: Typical 3–6 ECTS credits awarded upon completion. Eligibility = enrolled higher-education students (Bachelor / Master / PhD) at a sending institution that participates in Erasmus+. Language varies — most BIPs are in English; CEFR B2 is the most common minimum. 2–3 paragraphs.

    4. `id="find"` — **How to find one on BipHub**: One paragraph walking the reader through the filter sidebar at `/bips` (country, field, language, dates, ECTS, status, study level). Include an inline `<Link href="/bips" className="text-eu-blue underline">Browse all BIPs</Link>` call to action.

    5. `id="faq"` — **Frequently asked questions**: An Accordion with EXACTLY these 8 items (D-06, locked order):
       1. "Who can apply to a BIP?" — Answer: enrolled higher-education students at an Erasmus+-participating institution; specifics depend on the host BIP's eligibility notes.
       2. "How long does a BIP last?" — Answer: 5–10 days of in-person physical mobility, plus an asynchronous virtual component spanning several weeks.
       3. "Do I get ECTS credits?" — Answer: yes — typically 3–6 ECTS depending on the programme. Credits transfer through the standard Erasmus+ learning-agreement process at your home university.
       4. "What language is the BIP in?" — Answer: most BIPs run in English with a CEFR B2 minimum, but BipHub has German, French, Spanish, and Italian programmes too. Filter by language on `/bips`.
       5. "Are travel costs covered?" — Answer: Erasmus+ funds a daily living allowance (~€79/day at the time of writing) plus a travel grant. The home university disburses the funding. BipHub does not handle money.
       6. "Can I join from outside the EU?" — Answer: KA131 is for students enrolled at Erasmus+-participating institutions, most of which are in EU member states or programme-associated countries. Students from non-participating institutions cannot join through Erasmus+ funding.
       7. "How are BIPs different from regular Erasmus exchange?" — Answer: a full Erasmus+ semester is 3–12 months at the host. A BIP is a short intensive (5–10 days physical + virtual) for students who cannot commit a full semester abroad.
       8. "How do I apply through my home university?" — Answer: contact your home university's Erasmus / international office. They run the application, learning agreement, and grant. BipHub is a directory — applications are NOT submitted here.

       Use `<Accordion type="single" collapsible>` if that is the existing API (read accordion.tsx first to confirm — Phase 1 Plan 01-06 used `multiple` prop on @base-ui Accordion; replicate the exact prop signature already in the codebase). Each item: `<AccordionItem value="faq-N">`, trigger renders the question, content renders the answer (2–4 sentences each per D-06).

    6. Outbound EC link (D-04 footer of page, INFO-04 requirement): a `<section>` after the FAQ rendering:
       ```tsx
       <p className="mt-12 text-sm text-muted">
         <a
           href="https://erasmus-plus.ec.europa.eu/programme-guide/part-b/ka1/short-term-mobility-projects/blended-intensive-programmes"
           target="_blank"
           rel="noopener noreferrer"
           className="text-eu-blue underline"
         >
           Read the official Erasmus+ programme guide entry on BIPs
         </a>
       </p>
       ```
       Link text MUST be exactly "Read the official Erasmus+ programme guide entry on BIPs" (specifics block — no EC affiliation claim per CLAUDE.md).

    Layout shell:
    - Wrap content in `<div className="container mx-auto max-w-[1200px] px-4 lg:px-6 py-16 lg:py-24">`.
    - On desktop only (`hidden lg:block`), render a sticky jump-link sidebar (`<nav aria-label="Page sections" className="sticky top-24">`) with `<a>` anchors to `#what`, `#virtual-physical`, `#ects`, `#find`, `#faq`. Use a 2-column grid `grid grid-cols-1 lg:grid-cols-[200px_1fr] gap-12`.
    - Each section starts with `<Eyebrow>` label, then an `<h2 className="text-[clamp(28px,3.5vw,40px)] font-bold tracking-tight text-ink">` heading, then prose in `<div className="prose prose-lg max-w-none mt-4 text-ink-2 leading-relaxed">` style (use Tailwind utilities directly — DO NOT add `@tailwindcss/typography`; the project does not have it installed).

    Hard constraints (CLAUDE.md):
    - NO `'use client'` directive (RSC).
    - NO `<img>` tags (use `next/image` only if an image is needed; for v1 this page is text-only).
    - NO EU 12-star emblem references in copy or markup.
    - NO claims of EC affiliation — link text is "Read the official Erasmus+ programme guide entry on BIPs" only.
    - Tailwind class names MUST be literal strings; no template literals constructing class names dynamically.
    - All anchor links use `<Link>` from `next/link` for internal routes; outbound EC link uses native `<a>` with `target="_blank" rel="noopener noreferrer"`.

    Implements: INFO-01 (explainer covering KA131, virtual component, ECTS, eligibility), INFO-02 (FAQ section with 8 items per D-06), INFO-04 (outbound link to EC programme guide), per D-04 + D-05 + D-06 in 04-CONTEXT.md.
  </action>
  <acceptance_criteria>
    - File `app/(public)/what-is-a-bip/page.tsx` exists
    - `grep -q "export const dynamic = 'force-static'" "app/(public)/what-is-a-bip/page.tsx"` exits 0
    - `grep -q "export const metadata" "app/(public)/what-is-a-bip/page.tsx"` exits 0
    - `grep -c 'id="what"\|id="virtual-physical"\|id="ects"\|id="find"\|id="faq"' "app/(public)/what-is-a-bip/page.tsx"` returns 5
    - `grep -q "Who can apply to a BIP" "app/(public)/what-is-a-bip/page.tsx"` exits 0
    - `grep -q "How do I apply through my home university" "app/(public)/what-is-a-bip/page.tsx"` exits 0
    - `grep -c "AccordionItem" "app/(public)/what-is-a-bip/page.tsx"` returns 8 (one per FAQ entry)
    - `grep -q "erasmus-plus.ec.europa.eu/programme-guide/part-b/ka1/short-term-mobility-projects/blended-intensive-programmes" "app/(public)/what-is-a-bip/page.tsx"` exits 0
    - `grep -q "Read the official Erasmus+ programme guide entry on BIPs" "app/(public)/what-is-a-bip/page.tsx"` exits 0
    - `grep -q "rel=\"noopener noreferrer\"" "app/(public)/what-is-a-bip/page.tsx"` exits 0
    - `grep -v '^//\|^ \*\|^/\*' "app/(public)/what-is-a-bip/page.tsx" | grep -c "'use client'"` returns 0
    - `npm run build` exits 0 with `/what-is-a-bip` listed as a static route in the build summary
    - Manual: opening `/what-is-a-bip` in the dev server renders 5 section headings + 8 FAQ accordion entries + the EC link
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | grep -E "/what-is-a-bip"</automated>
  </verify>
  <done>
    /what-is-a-bip renders as a static route with 5 sections, 8-item FAQ accordion, and the verified EC outbound link.
    Footer link (already shipped) now resolves to a real page.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → /what-is-a-bip | Public read-only route; no untrusted input crosses any boundary |
| outbound link → erasmus-plus.ec.europa.eu | External EC site opened in new tab with noopener+noreferrer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T (Tampering) | Outbound EC link | mitigate | `rel="noopener noreferrer" target="_blank"` prevents window.opener tab-nabbing; outbound URL is hardcoded literal, not user-influenced |
| T-04-02 | I (Information disclosure) | EC affiliation claim | mitigate | Link text restricted to "Read the official Erasmus+ programme guide entry on BIPs" per CLAUDE.md — no implicit EC endorsement claim |
| T-04-03 | D (Denial of service) | EC programme guide URL drift | accept | EC may restructure the URL; verified during execution, no automated link-checker in v1 (deferred to v1.1) |
</threat_model>

<verification>
- `npm run build` succeeds; `/what-is-a-bip` shown as static route
- No TypeScript or ESLint errors introduced
- Dev server renders the page with all 5 section anchors and 8 FAQ items
- Browser DevTools confirms the EC outbound link has `target="_blank"` and `rel="noopener noreferrer"`
</verification>

<success_criteria>
- INFO-01 satisfied: page covers KA131 context, virtual component, ECTS, eligibility
- INFO-02 satisfied: FAQ section with 8 items rendered as an accordion
- INFO-04 satisfied: outbound link to EC programme guide present with correct rel attributes
- Page is statically generated (force-static), no runtime DB calls, no client JS in shell
- Existing footer link from Plan 01-04 now resolves to a real page
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-static-content-performance-hardening/04-01-SUMMARY.md`
</output>
