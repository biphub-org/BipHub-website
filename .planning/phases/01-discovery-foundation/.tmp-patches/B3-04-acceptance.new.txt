    - **B3 mobile-nav gate:** `components/ui/sheet.tsx` exists (installed via `npx shadcn@latest add sheet` in Task 1). `components/home/StickyNav.tsx` imports `Sheet`, `SheetTrigger`, `SheetContent`, `SheetClose` from `@/components/ui/sheet`. The hamburger `<button>` is wrapped in `<SheetTrigger asChild>` and the `<SheetContent>` renders `<Link>` elements for ALL of `/bips`, `/what-is-a-bip`, `/login`, `/register`. Mechanically: `grep -cE "SheetTrigger|SheetContent" components/home/StickyNav.tsx` ≥ 2 AND `grep -cE 'href="/(bips|what-is-a-bip|login|register)"' components/home/StickyNav.tsx` ≥ 4.
    - **B3 mobile-nav functional check:** at viewport 360px, click the hamburger button → the Sheet opens; pressing Escape closes it; tabbing inside the sheet cycles within the menu (focus trap); each menu link is reachable via keyboard.
    - `npm run build` exits 0
    - `npm run dev` and `curl -s http://localhost:3000 | grep -c "Independent project"` returns ≥1
    - `grep -rE "(\\b12\\b.*star|star.*\\b12\\b|length:[[:space:]]*12)" components/home/LogoMark.tsx` returns 0 matches (the EU 12-star prohibition)