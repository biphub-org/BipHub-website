---
phase: 02-coordinator-auth-submission
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00009_profiles_erasmus_code.sql
  - lib/supabase/database.types.ts
  - components/ui/input.tsx
  - components/ui/textarea.tsx
  - components/ui/form.tsx
  - components/ui/label.tsx
  - components/ui/checkbox.tsx
  - components/ui/switch.tsx
  - components/ui/tabs.tsx
  - components/ui/badge.tsx
  - components/ui/dialog.tsx
  - components/ui/popover.tsx
  - components/ui/command.tsx
  - components/ui/separator.tsx
  - components/ui/alert.tsx
  - lib/utils/status.ts
  - app/globals.css
autonomous: false
requirements: [AUTH-07]
must_haves:
  truths:
    - "profiles table has erasmus_code column populated for new coordinator profiles"
    - "Coordinator-callable SECURITY DEFINER function inserts new universities with server-side validation (no createAdminClient outside admin path)"
    - "lib/supabase/database.types.ts reflects the post-migration schema (profiles.erasmus_code present)"
    - "Phase 2 shadcn UI primitives (input, textarea, form, label, checkbox, switch, tabs, badge, dialog, popover, command, separator, alert) are installed"
    - "lib/utils/status.ts exposes a STATUS_BADGE_CLASSES lookup with full literal Tailwind class strings for draft/pending/approved/rejected"
  artifacts:
    - path: "supabase/migrations/00009_profiles_erasmus_code.sql"
      provides: "erasmus_code column on profiles + insert_university_if_not_exists() SECURITY DEFINER function"
      contains: "alter table public.profiles add column"
    - path: "lib/supabase/database.types.ts"
      provides: "Generated TypeScript types reflecting profiles.erasmus_code"
      contains: "erasmus_code"
    - path: "lib/utils/status.ts"
      provides: "STATUS_BADGE_CLASSES record (Tailwind v4 static lookup)"
      exports: ["STATUS_BADGE_CLASSES"]
    - path: "components/ui/input.tsx"
      provides: "shadcn Input primitive"
    - path: "components/ui/form.tsx"
      provides: "shadcn Form, FormField, FormItem, FormLabel, FormControl, FormMessage"
    - path: "components/ui/dialog.tsx"
      provides: "shadcn Dialog primitive"
    - path: "components/ui/command.tsx"
      provides: "shadcn Command primitive"
    - path: "components/ui/popover.tsx"
      provides: "shadcn Popover primitive"
  key_links:
    - from: "lib/utils/status.ts"
      to: "app/globals.css (status color tokens)"
      via: "Tailwind v4 @theme inline tokens --color-status-draft/pending/approved/rejected"
      pattern: "--color-status-(draft|pending|approved|rejected)(-bg)?:"
    - from: "lib/supabase/database.types.ts"
      to: "supabase/migrations/00009_profiles_erasmus_code.sql"
      via: "supabase gen types typescript --local"
      pattern: "erasmus_code"
---

<objective>
Land the foundational Phase 2 plumbing that every other plan depends on:
1. Migration `00009_profiles_erasmus_code.sql` adds `profiles.erasmus_code` and a SECURITY DEFINER `insert_university_if_not_exists(text, text, text)` function so coordinators can self-register universities without `createAdminClient` outside `app/(admin)/`.
2. The local Supabase database is reset against all migrations and `lib/supabase/database.types.ts` is regenerated so downstream plans get type safety on `profiles.erasmus_code` and the new RPC.
3. Phase 2 shadcn UI primitives are installed (`input textarea form label checkbox switch tabs badge dialog popover command separator alert`) — every downstream plan composes from these.
4. Phase 2 status color tokens are appended to `app/globals.css` and `lib/utils/status.ts` materializes the Tailwind v4 static class lookup.

Purpose: Without (1) and (2) the onboarding form (Plan 02-04) cannot save `erasmus_code` and TypeScript will fail. Without (3) the auth forms (Plan 02-02) cannot render. Without (4) `DashboardBipCard` (Plan 02-05) cannot color status badges (Tailwind v4 forbids template-literal class names — CLAUDE.md never-do).

Output: One additive migration, one regenerated types file, 13 new shadcn component files, one status utility module, extended `app/globals.css` `@theme` tokens.
</objective>

<execution_context>
@C:/dev/Antigravity/BIP_project/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/BIP_project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-coordinator-auth-submission/02-CONTEXT.md
@.planning/phases/02-coordinator-auth-submission/02-RESEARCH.md
@.planning/phases/02-coordinator-auth-submission/02-PATTERNS.md
@.planning/phases/02-coordinator-auth-submission/02-UI-SPEC.md
@CLAUDE.md
@supabase/migrations/00002_universities_profiles.sql
@supabase/migrations/00006_rls_policies.sql
@supabase/migrations/00008_app_metadata_role_mirror.sql
@app/globals.css
@lib/supabase/database.types.ts

<interfaces>
<!-- Existing profiles schema (pre-migration). Plan adds erasmus_code column. -->
From lib/supabase/database.types.ts (current — no erasmus_code):
```typescript
profiles: {
  Row: {
    contact_email: string | null
    created_at: string
    full_name: string | null
    id: string                      // FK to auth.users(id)
    role: string
    university_id: string | null
    updated_at: string
  }
  // Insert and Update types mirror Row
}
```

After migration regeneration, profiles Row/Insert/Update MUST include `erasmus_code: string | null`.

Existing universities Row (used by RPC return shape):
```typescript
universities: {
  Row: {
    id: string
    name: string
    country: string
    erasmus_code: string | null
    created_at: string
  }
}
```

New RPC contract (added by this plan):
```typescript
// Functions block in database.types.ts after regen:
insert_university_if_not_exists: {
  Args: { p_name: string; p_country: string; p_erasmus_code?: string }
  Returns: string  // uuid
}
```
</interfaces>

<existing_migration_pattern>
00008_app_metadata_role_mirror.sql is additive (REVOKE EXECUTE + backfill, no recreate). 00009 follows the same additive philosophy: new column on existing table, new function, no drops, no policy changes.
</existing_migration_pattern>

<existing_status_color_tokens_in_globals_css>
The `@theme inline` block already declares `--color-eu-blue`, `--color-eu-gold`, `--color-ink`, `--color-ink-2`, `--color-bg`, `--color-bg-soft`, `--color-border`, choropleth tier tokens. Phase 2 appends 8 new tokens (4 foreground, 4 background) for status badges. Do not redefine existing tokens.
</existing_status_color_tokens_in_globals_css>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 00009 (erasmus_code column + SECURITY DEFINER university insert function)</name>
  <files>supabase/migrations/00009_profiles_erasmus_code.sql</files>
  <read_first>
    - supabase/migrations/00002_universities_profiles.sql (profiles + universities table DDL; existing SECURITY DEFINER pattern at lines 43-60)
    - supabase/migrations/00006_rls_policies.sql (universities_insert_admin policy this function bypasses; profiles_insert_own with check id = auth.uid())
    - supabase/migrations/00008_app_metadata_role_mirror.sql (additive migration pattern reference)
    - lib/countries.ts (canonical list of Erasmus+ programme country codes — verify the list used in the function CHECK constraint matches)
    - .planning/phases/02-coordinator-auth-submission/02-RESEARCH.md (Pattern 5 — full SQL specification, lines 540-598)
  </read_first>
  <action>
Create `supabase/migrations/00009_profiles_erasmus_code.sql` containing exactly two changes (additive only — no drops, no policy modifications):

1. Add `erasmus_code` column to `public.profiles`:
```sql
alter table public.profiles add column if not exists erasmus_code text;
```

2. Create `public.insert_university_if_not_exists(p_name text, p_country text, p_erasmus_code text default null) returns uuid` as a `language plpgsql security definer set search_path = public` function. Body:
   - Trim and validate `p_name` length >= 2 → otherwise `raise exception 'University name too short'`.
   - Validate `p_country` against the Erasmus+ programme country code whitelist. **Source the canonical list from `lib/countries.ts`** — read that file first and copy the exact ISO codes into the SQL `in (...)` clause. The country whitelist MUST match the application-level Zod validation source-of-truth so a user cannot select a country the function rejects.
   - Case-insensitive existence check: `select id from public.universities where lower(trim(name)) = lower(trim(p_name)) and country = p_country limit 1`.
   - If not found, insert `(trim(p_name), p_country, nullif(trim(coalesce(p_erasmus_code,'')),''))` and return new id.
   - If found, return existing id.

3. After the function definition: `grant execute on function public.insert_university_if_not_exists(text, text, text) to authenticated;` (NOT to anon — only logged-in coordinators can self-register universities).

Header comment must explain:
   - This is additive (Phase 2): no recreates, no drops.
   - Decision rationale: SECURITY DEFINER function chosen over `createAdminClient` to keep service-role isolation rule (CLAUDE.md never-do, ESLint `no-restricted-imports`) intact.
   - The function bypasses `universities_insert_admin` RLS via SECURITY DEFINER + `set search_path = public` (search_path lock prevents schema-shadowing attacks).

Do NOT modify migrations 00001-00008.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const m=fs.readFileSync('supabase/migrations/00009_profiles_erasmus_code.sql','utf8'); const checks=[/alter table public\.profiles add column if not exists erasmus_code text/i.test(m), /create or replace function public\.insert_university_if_not_exists/i.test(m), /security definer/i.test(m), /set search_path = public/i.test(m), /grant execute on function public\.insert_university_if_not_exists\(text, text, text\) to authenticated/i.test(m), !/drop /i.test(m)]; if(checks.every(Boolean)){console.log('OK')}else{console.error('FAIL',checks);process.exit(1)}"</automated>
  </verify>
  <done>
    - supabase/migrations/00009_profiles_erasmus_code.sql exists
    - File contains: `alter table public.profiles add column if not exists erasmus_code text`
    - File contains: `create or replace function public.insert_university_if_not_exists`
    - File contains: `security definer` + `set search_path = public`
    - File contains: `grant execute on function public.insert_university_if_not_exists(text, text, text) to authenticated`
    - No `drop` statements anywhere in the file
    - Country whitelist `in (...)` clause uses ISO codes that exist in `lib/countries.ts`
  </done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply migrations + regenerate database.types.ts + add Phase 2 @theme tokens</name>
  <files>lib/supabase/database.types.ts, app/globals.css, lib/utils/status.ts</files>
  <read_first>
    - supabase/migrations/00009_profiles_erasmus_code.sql (just-written file)
    - app/globals.css (existing @theme inline block — append, do not redefine)
    - .planning/phases/02-coordinator-auth-submission/02-UI-SPEC.md (lines 49-70 — exact CSS token names + 62-70 safelist comment)
    - lib/supabase/database.types.ts (current generated shape — confirm the regen actually replaces this)
  </read_first>
  <action>
This task has THREE sequential steps. All must succeed.

**Step 1 — Apply migrations to local Supabase:**

Run `npx supabase db reset` from the repo root. This drops the local DB, re-applies all migrations 00001-00009, and re-runs `seed.sql`. Expected to exit 0. Confirm migration 00009 ran by inspecting output for `Applying migration 00009_profiles_erasmus_code.sql...`.

If `supabase start` is not running: run `npx supabase start` first, then `db reset`.

If `db reset` fails on the country whitelist (Task 1 may have used a code not present in `lib/countries.ts`), fix the SQL and re-run.

**Step 2 — Regenerate TypeScript types:**

Run `npx supabase gen types typescript --local > lib/supabase/database.types.ts` (or `npm run db:types` if that script exists in `package.json`).

Verify the regenerated file contains:
- `profiles: { Row: { ... erasmus_code: string | null ... } }` (and Insert/Update mirrors)
- A `Functions` block with `insert_university_if_not_exists: { Args: { p_name: string; p_country: string; p_erasmus_code?: string }; Returns: string }` (or compatible shape — supabase CLI may emit `Returns: string` for `uuid`).

If the regeneration removed any pre-existing types, that is expected — the file is fully regenerated by the CLI. Commit the new file as-is.

**Step 3 — Append Phase 2 status color tokens to `app/globals.css`:**

Inside the existing `@theme inline { ... }` block (do NOT create a new one — appending inside the existing block keeps Tailwind v4 happy), add these 8 tokens. Do not redefine any existing token; append at the end of the block, with a `/* Phase 2 — coordinator status colors */` section comment:

```css
/* Phase 2 — coordinator status colors */
--color-status-draft: #6b7390;
--color-status-pending: #b45309;
--color-status-approved: #15803d;
--color-status-rejected: #dc2626;
--color-status-draft-bg: #f7f8fc;
--color-status-pending-bg: #fffbeb;
--color-status-approved-bg: #f0fdf4;
--color-status-rejected-bg: #fef2f2;
```

These match UI-SPEC lines 52-60 exactly.

**Step 4 — Create `lib/utils/status.ts`:**

```typescript
/**
 * Status badge class lookup — Tailwind v4 static scanner safelist.
 *
 * NEVER use template literals like `bg-status-${status}-bg` — Tailwind v4 purges
 * dynamic strings (CLAUDE.md never-do; PITFALLS Pitfall 13).
 *
 * Tokens declared in app/globals.css `@theme inline` block (Phase 2 additions).
 */

export type BipStatus = 'draft' | 'pending' | 'approved' | 'rejected'

export const STATUS_BADGE_CLASSES: Record<BipStatus, string> = {
  draft:    'bg-status-draft-bg text-status-draft border-status-draft',
  pending:  'bg-status-pending-bg text-status-pending border-status-pending',
  approved: 'bg-status-approved-bg text-status-approved border-status-approved',
  rejected: 'bg-status-rejected-bg text-status-rejected border-status-rejected',
}

export const STATUS_LABELS: Record<BipStatus, string> = {
  draft: 'Draft',
  pending: 'Pending',
  approved: 'Approved',
  rejected: 'Rejected',
}
```

This task is **[BLOCKING]** — Plan 02-02 onwards reads `profiles.erasmus_code` from the regenerated types. If types regeneration fails, every downstream plan will fail TypeScript on `npm run build`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('lib/supabase/database.types.ts','utf8'); const g=fs.readFileSync('app/globals.css','utf8'); const s=fs.readFileSync('lib/utils/status.ts','utf8'); const checks=[/erasmus_code/.test(t), /insert_university_if_not_exists/.test(t), /--color-status-draft:/.test(g), /--color-status-pending:/.test(g), /--color-status-approved:/.test(g), /--color-status-rejected:/.test(g), /--color-status-draft-bg:/.test(g), /STATUS_BADGE_CLASSES/.test(s), /bg-status-draft-bg text-status-draft border-status-draft/.test(s), /bg-status-pending-bg text-status-pending border-status-pending/.test(s)]; if(checks.every(Boolean)){console.log('OK')}else{console.error('FAIL',checks);process.exit(1)}"</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    - `npx supabase db reset` exits 0 with migration 00009 applied
    - `lib/supabase/database.types.ts` contains `erasmus_code` and `insert_university_if_not_exists` Function entry
    - `app/globals.css` contains all 8 status color CSS variables under a Phase 2 section comment
    - `lib/utils/status.ts` exists, exports `STATUS_BADGE_CLASSES` and `STATUS_LABELS` records, uses literal class strings (no template literals)
    - `npx tsc --noEmit` exits 0 (no type errors against regenerated types)
  </done>
</task>

<task type="auto">
  <name>Task 3: Install Phase 2 shadcn UI primitives</name>
  <files>components/ui/input.tsx, components/ui/textarea.tsx, components/ui/form.tsx, components/ui/label.tsx, components/ui/checkbox.tsx, components/ui/switch.tsx, components/ui/tabs.tsx, components/ui/badge.tsx, components/ui/dialog.tsx, components/ui/popover.tsx, components/ui/command.tsx, components/ui/separator.tsx, components/ui/alert.tsx</files>
  <read_first>
    - components.json (shadcn CLI config; confirm style + iconLibrary + RSC mode)
    - components/ui/button.tsx (existing EU-branded shadcn primitive — confirms current shadcn pattern + cn import path)
    - components/ui/sheet.tsx (existing shadcn primitive — confirms base-ui style)
    - .planning/phases/02-coordinator-auth-submission/02-RESEARCH.md (Standard Stack table — exact list of components to install; lines 137-156)
    - .planning/phases/02-coordinator-auth-submission/02-UI-SPEC.md (Design System — lines 30-46)
  </read_first>
  <action>
Run a single shadcn install command for the 13 Phase 2 components:

```bash
npx shadcn@latest add input textarea form label checkbox switch tabs badge dialog popover command separator alert
```

If the CLI prompts to overwrite the existing `button.tsx`, **answer No** — that file is the EU-branded variant from Plan 01-04. If the CLI prompts about `lib/utils.ts`, accept defaults; both `lib/utils.ts` and `lib/utils/cn.ts` re-export the same `cn` (Plan 01-04 decision — leave as-is).

After install, verify each of the 13 component files exists in `components/ui/` and exports the expected shadcn names:
- `input.tsx` exports `Input`
- `textarea.tsx` exports `Textarea`
- `form.tsx` exports `Form, FormField, FormItem, FormLabel, FormControl, FormDescription, FormMessage, useFormField`
- `label.tsx` exports `Label`
- `checkbox.tsx` exports `Checkbox`
- `switch.tsx` exports `Switch`
- `tabs.tsx` exports `Tabs, TabsList, TabsTrigger, TabsContent`
- `badge.tsx` exports `Badge` (and may export `badgeVariants`)
- `dialog.tsx` exports `Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle, DialogTrigger, DialogClose`
- `popover.tsx` exports `Popover, PopoverContent, PopoverTrigger`
- `command.tsx` exports `Command, CommandDialog, CommandEmpty, CommandGroup, CommandInput, CommandItem, CommandList, CommandSeparator, CommandShortcut`
- `separator.tsx` exports `Separator`
- `alert.tsx` exports `Alert, AlertDescription, AlertTitle`

Run `npx tsc --noEmit` after install. The shadcn CLI sometimes adds peer deps (e.g. `@radix-ui/react-dialog`) — if missing, the CLI will install them automatically; do NOT manually `npm install` extra packages unless `tsc` fails with a "module not found" error pointing at a `@radix-ui/*` import inside one of the new files.

**Do NOT touch `components/ui/button.tsx` or `components/ui/sheet.tsx`** — Plan 01-04 customized them.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const files=['input','textarea','form','label','checkbox','switch','tabs','badge','dialog','popover','command','separator','alert']; const missing=files.filter(f=>!fs.existsSync('components/ui/'+f+'.tsx')); if(missing.length===0){console.log('OK')}else{console.error('MISSING',missing);process.exit(1)}"</automated>
    <automated>node -e "const fs=require('fs'); const checks=[/export.*Input/.test(fs.readFileSync('components/ui/input.tsx','utf8')), /export.*Form/.test(fs.readFileSync('components/ui/form.tsx','utf8')), /export.*Dialog/.test(fs.readFileSync('components/ui/dialog.tsx','utf8')), /export.*Command/.test(fs.readFileSync('components/ui/command.tsx','utf8')), /export.*Tabs/.test(fs.readFileSync('components/ui/tabs.tsx','utf8'))]; if(checks.every(Boolean)){console.log('OK')}else{console.error('FAIL',checks);process.exit(1)}"</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    - All 13 component files exist in `components/ui/`
    - `Input`, `Form`, `Dialog`, `Command`, `Tabs` are confirmed exported (representative spot checks)
    - `npx tsc --noEmit` exits 0
    - `components/ui/button.tsx` and `components/ui/sheet.tsx` are unmodified vs. Plan 01-04 versions
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Migration 00009 applied locally, types regenerated, 13 shadcn primitives installed, status color tokens live in `app/globals.css`, `lib/utils/status.ts` materialized.</what-built>
  <how-to-verify>
1. Check that the local Supabase has the new column:
   ```bash
   psql "$(npx supabase status -o env | grep DB_URL | cut -d= -f2- | tr -d '"')" -c "select column_name from information_schema.columns where table_schema='public' and table_name='profiles' and column_name='erasmus_code';"
   ```
   Expected output: a single row with `erasmus_code`.

2. Check the SECURITY DEFINER function is callable by an authenticated role:
   ```bash
   psql "$(npx supabase status -o env | grep DB_URL | cut -d= -f2- | tr -d '"')" -c "select proname, prosecdef from pg_proc where proname='insert_university_if_not_exists';"
   ```
   Expected: `prosecdef = t` (true).

3. Confirm types regenerated correctly: open `lib/supabase/database.types.ts`, search for `erasmus_code` and `insert_university_if_not_exists` — both must be present.

4. Confirm shadcn install: `ls components/ui/` should show the 13 new files plus the 9 existing ones (button, sheet, calendar, drawer, select, sonner, slider, accordion, skeleton).

5. Confirm Tailwind tokens compile: `npm run dev`, open http://localhost:3000/, open DevTools → Computed → search for any `bg-status-pending-bg` element — there will be none yet (no consumers in this plan), but `npm run build` should pass without "unknown utility" errors. Run `npm run build` to confirm.

6. Confirm seed data still intact: count seeded BIPs:
   ```bash
   psql "$(npx supabase status -o env | grep DB_URL | cut -d= -f2- | tr -d '"')" -c "select count(*) from bips where is_seed = true;"
   ```
   Expected: 20.
  </how-to-verify>
  <resume-signal>Type "approved" once all 6 verification steps pass, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Postgres (via PostgREST RPC) | Authenticated coordinators call `insert_university_if_not_exists` with arbitrary `(name, country, erasmus_code)` — function bypasses RLS via SECURITY DEFINER |
| build → Tailwind v4 scanner | Status class strings must be literal in source for the static scanner to retain them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | `insert_university_if_not_exists` country argument | mitigate | Function CHECK against fixed Erasmus+ programme country whitelist sourced from `lib/countries.ts` — rejects unknown ISO codes |
| T-02-01-02 | Tampering | `insert_university_if_not_exists` name argument (XSS / spam payload) | mitigate | `length(trim(p_name)) >= 2` server-side; case-insensitive existence check prevents trivial duplicate spam; React JSX escapes name on render (universities are rendered as text content downstream) |
| T-02-01-03 | Elevation of Privilege | SECURITY DEFINER function callable by anon | mitigate | `grant execute ... to authenticated` only — anon role cannot call; revoke from public is implicit (no public grant) |
| T-02-01-04 | Elevation of Privilege | SECURITY DEFINER search_path attack | mitigate | `set search_path = public` locks the function's resolution scope so `name` cannot be hijacked by a session-set search_path |
| T-02-01-05 | Tampering | `profiles.erasmus_code` column added without UPDATE policy adjustment | accept | Existing `profiles_update_own_or_admin` RLS policy from migration 00006 already covers UPDATE on profiles — no new policy needed because the column is in the same table and inherits the existing USING + WITH CHECK |
| T-02-01-06 | Spoofing | Tailwind status class purge (T-02-01-06 ≠ a runtime threat but a build-time correctness bug) | mitigate | Static lookup in `lib/utils/status.ts` with full literal class strings — Tailwind v4 scanner finds them at build time (CLAUDE.md never-do compliance) |
| T-02-01-07 | Information Disclosure | `pg_proc` listing leaks function existence | accept | Function name is non-secret; PostgREST exposes RPC catalog by design; `grant execute to authenticated` is the access control |
| T-02-01-08 | Denial of Service | Coordinator floods `insert_university_if_not_exists` with junk inserts | accept | Phase 2 risk acceptance: low likelihood (coordinators are real institutions); admin can clean up universities table; v2 rate limiting deferred (RESEARCH.md Pitfall 5 + GROW-01) |
</threat_model>

<verification>
- Migration 00009 contains the column add + SECURITY DEFINER function + grant.
- `npx supabase db reset` exits 0; all 9 migrations + seed.sql apply cleanly.
- `lib/supabase/database.types.ts` contains `erasmus_code` and `insert_university_if_not_exists`.
- `app/globals.css` contains 8 new status CSS variables; existing tokens unchanged.
- `lib/utils/status.ts` exports `STATUS_BADGE_CLASSES` with literal class strings.
- All 13 shadcn primitives are installed and tsc-clean.
- `npm run build` exits 0 (Tailwind v4 scanner picks up the new tokens via the literal class lookup).
- 20 seeded BIPs still in DB after `db reset`.
</verification>

<success_criteria>
- AUTH-07 unblocked: `profiles.erasmus_code` exists and is typed; `insert_university_if_not_exists` RPC is callable from a server action with the anon-key client.
- All downstream Phase 2 plans (02-02..02-07) inherit a working DB schema, a typed Supabase client, the shadcn UI primitives they need, and the status badge class lookup.
- Zero `createAdminClient` introductions outside `app/(admin)/` and `lib/supabase/admin.ts` (CLAUDE.md never-do preserved).
</success_criteria>

<output>
After completion, create `.planning/phases/02-coordinator-auth-submission/02-01-SUMMARY.md` documenting:
- Migration 00009 applied; column + function shape verified.
- Types regenerated; capture the diff size.
- shadcn primitives installed; capture peer-dep additions to `package.json` if any.
- Tailwind status tokens live; capture token names.
- Status utility exported; capture lookup keys.
- Threat register dispositions confirmed.
</output>
